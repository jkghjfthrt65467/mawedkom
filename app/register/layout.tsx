import { noindexMeta } from "@/lib/seo";

export const metadata = noindexMeta("إنشاء حساب");

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
