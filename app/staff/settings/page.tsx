import { StaffPortal } from "@/components/staff/StaffPortal";

export const metadata = { title: "ضبط المشروع" };

export default function StaffSettingsPage() {
  return <StaffPortal section="settings" />;
}
