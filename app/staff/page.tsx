import { StaffPortal } from "@/components/staff/StaffPortal";

export const metadata = { title: "بوابة الموظف" };

export default function StaffPage() {
  return <StaffPortal section="calendar" />;
}
