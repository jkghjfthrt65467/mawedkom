import { noindexMeta } from "@/lib/seo";

export const metadata = noindexMeta("استعادة الحساب");

export default function ForgotLayout({ children }: { children: React.ReactNode }) {
  return children;
}
